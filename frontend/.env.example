# Base URL for the CallVitals Pro backend API. In local dev this points at
# the backend's dev server; in the Docker image it's baked to "/api" and
# resolved by nginx's reverse proxy (see frontend/nginx.conf).
VITE_API_BASE_URL=http://localhost:4000/api
